rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Source snapshots and migration artifacts are private institutional data.
    match /{object=**} {
      allow read, write: if false;
    }
  }
}
